spring:
  datasource:
    url: ${JAVA_ADMIN_STARTER_DATASOURCE_URL:jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_UPPER=false}
    username: ${JAVA_ADMIN_STARTER_DATASOURCE_USERNAME:sa}
    password: ${JAVA_ADMIN_STARTER_DATASOURCE_PASSWORD:}
  flyway:
    enabled: true

platform:
  iam:
    jwt-secret: test-only-secret-do-not-use-in-production-32chars
  file:
    storage:
      type: local
      zone-id: Asia/Shanghai
      local:
        root-dir: ${java.io.tmpdir}/java-admin-starter/test-uploads
        base-url: /local-files

mybatis-plus:
  global-config:
    db-config:
      logic-delete-value: unix_timestamp()
      logic-not-delete-value: 0
