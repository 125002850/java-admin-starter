spring:
  application:
    name: java-demo
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  pattern:
    level: "%5p [traceId:%X{traceId:-}]"
