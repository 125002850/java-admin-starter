spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/java_demo_test?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai&createDatabaseIfNotExist=true
    username: root
    password: root

platform:
  file:
    storage:
      type: ${FILE_STORAGE_TYPE:minio}
      zone-id: ${FILE_STORAGE_ZONE_ID:Asia/Shanghai}

logging:
  level:
    root: WARN
    com.demo: INFO
